# Creating a class

class User: #PascalCase (in Python we'll see PascalCase and snake_case for everything else, not really camelCase)
	pass

user_1 = User()


# We could just define the attributes with no constructor, but it's best not to.
user_1.id = "001"
user_1.username = "angela"



class User:
	def __init__(self, variable1, user_id):
		#initialize attributes
		self.id = user_id
		self.username = username
		self.followers = 0 # Example if initializing Instagram user. 
		self.following = 0	
	
	def follow(self, user):
		user.followers += 1
		self.following += 1

user_1 = User("001", "angela")
print(user_1.id) # Results in "001"

user_2 = User("002", "jack")

user_1.follow(user_2)

print(user_1.followers) # 0
print(user_1.following) # 1
print(user_2.followers) # 1
print(user_2.following) # 0