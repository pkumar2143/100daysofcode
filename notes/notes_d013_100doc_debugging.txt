# Debugging

# 1. Describe the Problem
def my_function():
	for i in range(1, 20):
		if i == 20:
			print("Yes")
			
# i never reaches 20...

# 2. Reproduce the Bug
# This becomes important when, for example, you run code a few times, it works, but on some instances it doesn'tell
from random import randint
dice_imgs = ["1",...,"6"]
dice_num = randint(a:1, b:6)
print(dice_imgs[dice_num])

# There is no index 6, the list HAS 6 items, but indices go from 0 to 5, but also a cannot be 1 otherwise we'll never get the first dice_img.
dice_num = randint(a:0, b:6)

# How to change the code so that it always produces this error.
# First find the instance of dice_num, which is the only variable that changes run to run, independently.
# Use th(at/ose) value(s) of dice_num.

# 3. Play computer
# Imagine an input and follow it through.
if 1980 < yr < 1994:
    print("Millenial")
elif yr > 1994:
    print("Gen Z")
    
# Never hits 1994.

# 4. Fix the errors, watch red lines

# 5. Print statements

# 6. Using a debugger
# Breakpoints, step thru, step into (defined libraries), step into my (our own code)